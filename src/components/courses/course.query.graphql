query Course($id: String!) {
  course(id: $id)

  courseUnits(id: $id)

  courseReport(id: $id)

  units {
    blockCount
    id
    name
    dynamic
    topics
  }

  acs

  topics {
    id
    name
  }
}
