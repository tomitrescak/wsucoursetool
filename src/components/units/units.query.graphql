query Units {
  topics {
    id
    name
  }

  units {
    blockCount
    dynamic
    topics {
      id
      ratio
    }
    id
    name
    level
    outdated
    obsolete
    processed
    proposed
    contacted
    fixed
  }
}
